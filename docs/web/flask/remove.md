# Removing Session Data

Remove single keys or clear all session data.

```python
session.pop('theme', None)
```

```python
session.clear()
```

Use `session.clear()` on logout to fully reset user state.

