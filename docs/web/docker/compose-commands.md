# Compose Commands

```bash
docker compose up
docker compose up --build
docker compose down
docker compose ps
docker compose logs -f
docker compose exec web bash
```

Use `down` to stop and remove containers created by Compose.
