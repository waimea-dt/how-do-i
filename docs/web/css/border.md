# Border Properties

Borders can separate content and show state.

```css
.input {
    border: 1px solid #cbd5e1;
    border-radius: 0.5rem;
}

.input:focus {
    border-color: #0284c7;
    outline: none;
}
```

Use border state changes to show focus and validation.
