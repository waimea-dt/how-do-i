# What is Grid?

CSS Grid is two-dimensional layout system.
Use it when you need rows and columns together.

```css
.gallery {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
}
```

Great for dashboards, galleries, and card layouts.
