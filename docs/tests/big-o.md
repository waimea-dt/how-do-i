# Big-O Complexity Growth

Visualise how different algorithm complexity classes grow as input size increases.

## Default — n up to 15

Drag the slider right to watch the curves diverge. The exponential curve dominates and the others are compressed towards the x-axis.

<big-o></big-o>

## Pre-set slider value

<big-o value="5"></big-o>
<big-o value="2"></big-o>

## Only O(1) enabled

<big-o enabled="o1"></big-o>

## Only O(1), O(n), O(n^3) enabled

<big-o enabled="o1 on on3"></big-o>

## Only O(log n), O(n log n), O(n²), O(2ⁿ), O(n!) enabled

<big-o enabled="ologn onlogn on2 o2n ofact"></big-o>

